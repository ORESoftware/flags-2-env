[package]
name = "flags2env-typed-example"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
flags2env = { path = "../../clients/rust" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[build-dependencies]
flags2env-build = { path = "../../clients/rust-build" }

[workspace]
