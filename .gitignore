build/
dist/
node_modules/
target/
clients/rust/Cargo.lock
clients/*/.gradle/
.dart_tool/
clients/dart/pubspec.lock
__pycache__/
*.log
.gradle/
**/.gradle/
.build/
.zig-cache/
.cache/
.direnv/
tmp
temp
tmp/worktrees
temp/worktrees
.zed/

# BEGIN ORESoftware plaintext dotenv boundary
# Real plaintext dotenv state is local-only. Synthetic parser fixtures are
# tracked as fixture.dotenv and may materialize .env only during test runtime.
*.env
*/*.env
*/**/*.env
.env.*
*.env.*
!.env.example
!clients/browser/toolchain.env
# END ORESoftware plaintext dotenv boundary

# DEN-2846 prebuilt harness staging output (certified artifacts land via DEN-2848)
/build/prebuilt-staging/
