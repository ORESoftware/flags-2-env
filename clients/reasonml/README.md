# Flags2Env ReasonML

ReasonML facade for the `flags2env` OCaml bindings.

The opam package publishes as `flags2env-reason` and depends on the OCaml
`flags2env` package instead of reusing the same opam package name.
