# Flags2Env Erlang

Erlang bindings for the `flags2env` native parser.

The Hex package includes the Erlang facade, NIF source, and package-local
`parser.c`/`parser.h` sources needed to build the native parser.
