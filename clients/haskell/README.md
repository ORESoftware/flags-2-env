# flags2env Haskell

Haskell bindings for an installed `flags2env` native library.

The package links with `-lflags2env`; set your platform library search path so
the loader can find `libflags2env.so`, `libflags2env.dylib`, or `flags2env.dll`.
