# Flags2Env Nim

Nim bindings for the `flags2env` native parser.

Build or install the `flags2env` native shared library for your platform, then
make it discoverable through the platform library path before importing this
Nimble package.
