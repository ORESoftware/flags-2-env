OBJECTS = flags2env_r.o parser.o

PARSER_SRC = parser.c
PARSER_HEADER = parser.h
PKG_CPPFLAGS = -I.

parser.o: $(PARSER_SRC) $(PARSER_HEADER)
	$(CC) $(ALL_CPPFLAGS) $(ALL_CFLAGS) -fPIC -c $(PARSER_SRC) -o parser.o
