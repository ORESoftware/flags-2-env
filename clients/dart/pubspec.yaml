name: flags2env
version: 0.1.0
description: Dart FFI bindings for flags2env, a native parser for project-declared CLI flags.
repository: https://github.com/ORESoftware/flags-2-env
issue_tracker: https://github.com/ORESoftware/flags-2-env/issues

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  ffi: ^2.1.0
