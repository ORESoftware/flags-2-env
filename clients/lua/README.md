# Flags2Env Lua

LuaJIT FFI bindings for the `flags2env` native parser.

Set `FLAGS2ENV_NATIVE_LIB` or pass a library path to `Flags2Env.new` when the
native shared library is not discoverable from the default platform library
path.
