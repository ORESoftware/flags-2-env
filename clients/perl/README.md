# Flags2Env Perl

Perl bindings for an installed `flags2env` native library.

Set `FLAGS2ENV_NATIVE_LIB` or pass `library_path` to `Flags2Env->new` when the
native shared library is not discoverable from the default platform library
path.
